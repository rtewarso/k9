
### Installation/Running
python3 -m venv .venv

source .venv/bin/activate

pip install vosk

pip install sounddevice

pip install -r requirements

pip install ollama

(Start ollama externally)


llama3.1:8b on a 4070 works the best so far
